#include "identify.hpp"

int main()
{

	// generate();
	// generate();
	// generate();
	// generate();
	// generate();
	// generate();
	// generate();
	identify(*generate());
	identify(*generate());
	identify(*generate());
	identify(*generate());
	// identify(*generate());
	// identify(*generate());
	// identify(*generate());
	// identify(*generate());
	// identify(*generate());
	// Base *base = new ClassA;
	// std::cout << dynamic_cast<ClassA*>(*base);
}
